{% extends 'base.html.twig' %}

{% block title %}TypeProperty{% endblock %}

{% block body %}
    <h1>TypeProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_property.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_property.name }}</td>
            </tr>
            <tr>
                <th>Is_property</th>
                <td>{{ type_property.isProperty ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_property_index') }}">back to list</a>

    <a href="{{ path('type_property_edit', {'id': type_property.id}) }}">edit</a>

    {{ include('type_property/_delete_form.html.twig') }}
{% endblock %}
