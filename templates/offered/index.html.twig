{% extends 'base.html.twig' %}

{% block title %}Offered index{% endblock %}

{% block body %}
    <h1>Offered index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for offered in offereds %}
            <tr>
                <td>{{ offered.id }}</td>
                <td>{{ offered.created ? offered.created|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('offered_show', {'id': offered.id}) }}">show</a>
                    <a href="{{ path('offered_edit', {'id': offered.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('offered_new') }}">Create new</a>
{% endblock %}
