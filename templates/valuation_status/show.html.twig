{% extends 'base.html.twig' %}

{% block title %}ValuationStatus{% endblock %}

{% block body %}
    <h1>ValuationStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ valuation_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ valuation_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('valuation_status_index') }}">back to list</a>

    <a href="{{ path('valuation_status_edit', {'id': valuation_status.id}) }}">edit</a>

    {{ include('valuation_status/_delete_form.html.twig') }}
{% endblock %}
