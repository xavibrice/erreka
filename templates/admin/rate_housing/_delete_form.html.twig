<form method="post" action="{{ path('rate_housing_delete', {'id': rate_housing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rate_housing.id) }}">
    <button class="btn">Delete</button>
</form>
