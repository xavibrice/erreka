{% extends 'admin/layout.html.twig' %}

{% block body %}
{#    {{ form_start(form, {'action': path('property_new'), 'method': 'POST'}) }}#}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
{% endblock %}