{% extends 'admin/layout.html.twig' %}

{% block title %}Editar {{ company.name }}{% endblock %}

{% block content %}
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h1 class="h3 mb-4 text-gray-800 float-left">Editar Compañia</h1>
            <a class="btn btn-success float-right" href="{{ path('company_new') }}">Create new</a>
        </div>
        <div class="card-body">
            {{ include('admin/_form.html.twig', {'button_label': 'Actualizar'}) }}

            <a class="btn btn-link float-left mr-1" href="{{ path('company_index') }}">Volver a la lista</a>

            {{ include('admin/_delete_form.html.twig', {'route': 'company_delete', 'entity': company}) }}

        </div>
    </div>
{% endblock %}