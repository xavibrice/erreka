{% extends 'admin/layout.html.twig' %}

{% block title %} Listado noticias {% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_script_tags('dataTable') }}
{% endblock %}

{% block content %}
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            {#        <h6 class="m-0 font-weight-bold text-primary">DataTables Example</h6>#}
            <h1 class="h3 mb-4 text-gray-800 float-left">Listado de noticias a desarrollar</h1>
{#            <a class="btn btn-success float-right" href="{{ path('news_new') }}">Crear Noticia</a>#}
            <a class="btn btn-success float-right" href="#" data-toggle="modal" data-target="#createNewModal">
                Crear Noticia
            </a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped" id="dataTable" width="100%" cellpadding="0">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Estado</th>
                            <th>Creado</th>
                            <th>Zona</th>
                            {% if is_granted('ROLE_ADMIN') %}
                                <th>Asesor</th>
                            {% endif %}
                            <th>Situación</th>
                            <th>Motivo</th>
                            <th>Calle</th>
                            <th>Portal</th>
                            <th>Piso</th>
                            <th>Propietario</th>
                            <th>Móvil</th>
                            <th>Precio</th>
                            <th>Últ Llamada</th>
                            <th>Comentario</th>
{#                            <th>Valoración</th>#}
                            <th>Ver</th>
                            <th>Editar</th>
                            <th>Eliminar</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for new in news %}
                        <tr>
                            <td>{{ new.id }}</td>
                            <td>
                                {{ new.state }}
                            </td>
                            <td>
                                {{ new.created|date('d-m-Y') }}
                            </td>
                            <td>
                                <a href="{{ path('zone_show', {'id': new.street.zone.id }) }}">
                                    {{ new.street.zone.name }}
                                </a>
                            </td>
                            <td>
                                <a href="{{ path('user_show', {'id': new.commercial.id}) }}">
                                    {{ new.commercial.firstName }} {{ new.commercial.lastName }}
                                </a>
                            </td>
                            <td> {{ new.reason.situation.name }} </td>
                            <td>
                                {{ new.reason.name }}
                            </td>
                            <td>
                                {{ new.street.name }}
{#                                {% if new.zone.streets %}#}
{#                                    {{ new.zone.streets.count }}#}
{#                                {% endif %}#}
                            </td>
                            <td>{{ new.portal }}</td>
                            <td>{{ new.floor }}</td>
                            <td>
                                {{ new.firstName }} {{ new.lastName }}
                            </td>
                            <td>{{ new.mobile }} </td>
                            <td>{{ new.price|number_format(2, '.', ',') }} €</td>
                            <td>

                                {{ new.noteNews.last }}
                        {#        {% for noteNew in new.noteNews %}
                                    {{ noteNew.noticeDate|date }}
                                {% endfor %}#}
                            </td>
                            <td data-toggle="tooltip" data-placement="top" title="{{ new.comment }}">
                                {{ new.comment|slice(0, 25) }}
                            </td>
                            <td>
                                {{ include('admin/_show_link.html.twig', {'route': 'news_to_develop_show', 'id': new.id }) }}
{#                                <a class="btn btn-primary btn-circle" href="{{ path('news_edit_new', {'id': new.id}) }}"><i class="fas fa-eye"></i></a>#}
                            </td>
                            <td>
                                {{ include('admin/_edit_link.html.twig', {'route': 'news_edit_new', 'id': new.id }) }}
                            </td>
                            <td>
                                {{ include('admin/_delete_form.html.twig', {'route': 'news_to_develop_delete', 'entity': new, 'count': 0}) }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="13">No hay datos</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="modal fade" id="createNewModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Noticias a desarrollar</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-success m-3">{{ button_label|default('Guardar') }}</button>
                    {{ form_end(form) }}
{#                    {{ include('admin/news/_form_news.html.twig') }}#}
{#                    {{ render_esi(controller('App\\Controller\\NewsController::newForm')) }}#}

                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
