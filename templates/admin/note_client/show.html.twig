{% extends 'base.html.twig' %}

{% block title %}NoteClient{% endblock %}

{% block body %}
    <h1>NoteClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note_client.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ note_client.comment }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ note_client.created ? note_client.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('note_client_index') }}">back to list</a>

    <a href="{{ path('note_client_edit', {'id': note_client.id}) }}">edit</a>

    {{ include('note_client/_delete_form.html.twig') }}
{% endblock %}
